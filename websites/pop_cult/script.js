$(document).ready(function(){

for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var z = Math.max(0, Math.min(60, Math.ceil(Math.random() * 100)));
    $(".one").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".two").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".three").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".four").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".five").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".six").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".seven").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".eight").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".nine").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".ten").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".eleven").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".twelve").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".thirteen").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".fourteen").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".fifteen").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".sixteen").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".seventeen").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".eighteen").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".nineteen").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".twenty").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".twentyone").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".twentytwo").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".twentythree").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".twentyfour").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".twentyfive").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".twentysix").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".twentyseven").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".twentyeight").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".twentynine").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".thirty").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".thirtyone").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".thirtytwo").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".thirtythree").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}
for (var i=1; i <= 3; i++) {
    var x = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    var y = Math.max(0, Math.min(75, Math.ceil(Math.random() * 100)));
    $(".thirtyfour").css({
        position: 'absolute',
        top: y + '%',
        left: x + '%',
    });
}

$(function(){
	$(".grab").draggable();
});

$(".grab").click(function(){
	$(this).css('z-index', '1');
})

$(".grab").click(function(){
	$(".grab").not(this).css('z-index', '-1');
})

$(document).click(function(){
        $(".message").hide();
    });

});